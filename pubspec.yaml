name: kaam_dhanda_app
description: KaamDhanda - Job Portal App for India

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  url_launcher: ^6.2.5
  path_provider: ^2.1.1
  intl: ^0.18.1
  geolocator: ^10.1.0
  google_fonts: ^6.2.1
  share_plus: ^7.2.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
